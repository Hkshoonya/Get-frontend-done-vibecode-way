# Claude Code / Anthropic — UI metadata for skill discovery and chips
# Used by Claude Code and compatible runtimes when listing or invoking this skill.

display_name: Get frontend done (vibecode way)
short_description: Front-end UI skill grounded in The Component Gallery; feedback loop, memory, /simplify, /batch, GSD-ready.
default_prompt: Use the get-frontend-done-vibecode-way skill: Select component(s) from Component Gallery or references, Implement with semantics and accessibility, optionally /simplify; Verify; Iterate and optionally update Claude memory. For migrations use /batch.

compatibility:
  - Claude Code
  - Cursor
